const c=document.querySelector('#game'),g=c.getContext('2d'),hud=document.querySelector('#hud'),btn=document.querySelector('#start'),keys={};let p,things,score,running,last;
function resize(){c.width=c.clientWidth*devicePixelRatio;c.height=c.clientHeight*devicePixelRatio;g.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}addEventListener('resize',resize);resize();
function reset(){p={x:c.clientWidth/2,y:c.clientHeight/2,r:10};things=[];score=0;running=true;hud.textContent='0';btn.textContent='Restart'}
function spawn(){let good=Math.random()<.28;things.push({x:Math.random()*c.clientWidth,y:-15,r:good?7:10,v:90+Math.random()*100+score*2,good})}
function loop(t){let dt=Math.min(.03,(t-(last||t))/1000);last=t;let w=c.clientWidth,h=c.clientHeight;if(running){let dx=(keys.ArrowRight||keys.KeyD?1:0)-(keys.ArrowLeft||keys.KeyA?1:0),dy=(keys.ArrowDown||keys.KeyS?1:0)-(keys.ArrowUp||keys.KeyW?1:0);p.x=Math.max(p.r,Math.min(w-p.r,p.x+dx*230*dt));p.y=Math.max(p.r,Math.min(h-p.r,p.y+dy*230*dt));if(Math.random()<dt*(2.2+score/20))spawn();things.forEach(o=>o.y+=o.v*dt);things=things.filter(o=>{let hit=Math.hypot(o.x-p.x,o.y-p.y)<o.r+p.r;if(hit&&o.good){score++;hud.textContent=score;return false}if(hit){running=false;btn.textContent='Try again';return false}return o.y<h+20})}g.fillStyle='#09061a';g.fillRect(0,0,w,h);g.strokeStyle='#23164a';for(let q=0;q<w;q+=40){g.beginPath();g.moveTo(q,0);g.lineTo(q,h);g.stroke()}for(let q=0;q<h;q+=40){g.beginPath();g.moveTo(0,q);g.lineTo(w,q);g.stroke()}things?.forEach(o=>{g.fillStyle=o.good?'#55efff':'#ff416c';g.shadowBlur=18;g.shadowColor=g.fillStyle;g.beginPath();g.arc(o.x,o.y,o.r,0,7);g.fill()});if(p){g.fillStyle='#ffe66b';g.shadowColor='#ffe66b';g.shadowBlur=20;g.beginPath();g.moveTo(p.x,p.y-13);g.lineTo(p.x+11,p.y+10);g.lineTo(p.x-11,p.y+10);g.closePath();g.fill()}g.shadowBlur=0;requestAnimationFrame(loop)}addEventListener('keydown',e=>keys[e.code]=1);addEventListener('keyup',e=>keys[e.code]=0);btn.onclick=reset;requestAnimationFrame(loop);
